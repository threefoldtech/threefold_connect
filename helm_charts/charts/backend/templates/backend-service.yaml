apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{ include "backend.FULL_NAME" . }}
  name: {{ include "backend.FULL_NAME" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - protocol: "TCP"
      port: {{ .Values.service.port }}
      targetPort: 3001
  selector:
    app: {{ include "backend.FULL_NAME" . }}
